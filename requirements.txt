# Pinned to the versions this app is developed/tested against, for reproducible
# installs. Bump deliberately, not implicitly.

Flask==3.1.3
python-dotenv==1.2.1
gunicorn==25.1.0
Werkzeug==3.1.6

# RAG pipeline
chromadb==1.5.2
langchain-text-splitters==1.1.1

# Web scraping
requests==2.32.5
beautifulsoup4==4.14.3

# Ollama client
ollama==0.6.1

# Rate limiting
flask-limiter==4.1.1

# Testing
pytest>=8.0,<9
